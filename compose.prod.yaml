# Configuración de producción para app-dao-ops
services:
  db:
    ports: !reset []
    restart: always

  web:
    ports:
      - "8081:3000"
    restart: always
    environment:
      NODE_ENV: production

