import { TvClient } from "../tv-client";

// Vista de Órdenes para el televisor (anterior / actual / próximas).
export const dynamic = "force-dynamic";

export default function TvOrdenesPage() {
  return <TvClient />;
}
